<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <!-- Thêm liên kết đến Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <div class="container mt-5">
    <!-- Hiển thị thông báo lỗi hoặc thành công -->
    <div class="alert alert-danger" role="alert" style="display: <%= 'block' if flash[:alert] %>;">
      <%= flash[:alert] %>
    </div>
    <div class="alert alert-success" role="alert" style="display: <%= 'block' if flash[:notice] %>;">
      <%= flash[:notice] %>
    </div>

    <h2 class="text-center">Sign In</h2>

    <%= form_with url: session_path, class: "mt-4" do |form| %>
      <div class="form-group">
        <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: "Enter your email address", class: "form-control", value: params[:email_address] %>
      </div>
      <div class="form-group">
        <%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: "Enter your password", class: "form-control", maxlength: 72 %>
      </div>
      <div class="form-group text-center">
        <%= form.submit "Sign in", class: "btn btn-primary" %>
      </div>
    <% end %>

    <br>

    <div class="text-center mt-4">
      <%= link_to "Forgot password?", new_password_path, class: "d-block mb-2" %>
      <%= link_to "Create Account", new_user_path, class: "btn btn-secondary" %>
    </div>
  </div>

  <!-- Thêm liên kết đến Bootstrap JS và jQuery -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
