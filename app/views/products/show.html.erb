<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container my-5">
    <!-- Header -->
    <header class="text-center mb-5">
      <% cache @product do %>
        <h1 class="text-primary fw-bold mb-3"><%= @product.name %></h1>
        <p class="text-muted"><%= @product.description %></p>
      <% end %>
    </header>

    <!-- Featured Image -->
    <% if @product.featured_image.attached? %>
      <div class="text-center mb-4">
        <%= image_tag @product.featured_image, class: "img-fluid rounded shadow-sm", alt: @product.name %>
      </div>
    <% end %>

    <!-- Action Buttons -->
    <div class="text-center mb-4">
      <%= link_to "Back", products_path, class: "btn btn-secondary btn-sm m-2" %>
      <% if authenticated? %>
        <%= link_to "Edit", edit_product_path(@product), class: "btn btn-warning btn-sm m-2" %>
        <%= button_to "Delete", @product, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "btn btn-danger btn-sm m-2" %>
      <% end %>
    </div>

    <!-- Inventory Section -->
    <div class="text-center">
      <%= render "inventory", product: @product %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
